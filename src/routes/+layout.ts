export const prerender = true;

// SSR stays ON: the static adapter prerenders real content into index.html so
// crawlers and social previews see the portfolio itself, not an empty shell.
export const ssr = true;
